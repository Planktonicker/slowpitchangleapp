# Golden vectors generated by spike/gen_parity_fixtures.py — never edit by hand.
# Marked generated so GitHub collapses it in diffs and leaves it out of the
# repository's language statistics.
app/Tests/Fixtures/parity.json linguist-generated=true

# The verbatim FSF licence text is not this project's own code.
LICENSE linguist-vendored=true

# Footage must never reach the repository; if one ever slips past .gitignore,
# at least treat it as binary rather than trying to diff it.
*.mov binary
*.mp4 binary
*.m4v binary
